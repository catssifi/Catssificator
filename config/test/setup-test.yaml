common:
  #it can be either dev or prod
  run_mode: 'dev'

  #Of relative path format	
  file_upload_base_dir: 'catssificator-upload-test' 

cache:
  #The cache expiration setting in seconds 
  submissions_today: '60'
  submissions_in_the_past_n_days: '7200'